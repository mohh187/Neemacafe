{
  "name": "neemacafe",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "vercel-build": "npm run build",
    "img:upload": "node scripts/optimize-and-upload.mjs",
    "img:rewrite": "node scripts/update-menu-data.mjs",
    "deploy:netlify": "netlify deploy --build --prod --dir=${BUILD_DIR:-dist}",
    "deploy:cf": "wrangler pages deploy ${BUILD_DIR:-dist}"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "cloudinary": "^2.5.0",
    "dotenv": "^16.4.5",
    "sharp": "^0.33.3"
  },
  "devDependencies": {
    "netlify-cli": "^17.34.5",
    "wrangler": "^3.80.0"
  }
}
